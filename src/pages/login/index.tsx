import { View } from '@tarojs/components';
import React, { FC } from 'react';
import styles from './index.module.scss';

/**
 * 登录
 * @returns
 */
const Index: FC = () => {
  return (
    <View className={styles.layout}>
      <View className={styles.hader}></View>
    </View>
  );
};

export default Index;
