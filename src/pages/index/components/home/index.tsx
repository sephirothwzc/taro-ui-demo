import { View } from '@tarojs/components';
import React, { FC } from 'react';
import styles from './index.module.scss';

const Index: FC = () => <View className={styles.layout}>我是工作台</View>;

export default Index;
